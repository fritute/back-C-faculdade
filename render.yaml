services:
  - type: web
    name: distribpro-backend
    env: docker
    envVars:
      - key: DATABASE_URL
        sync: false   # defina o valor manualmente no painel do Render (não commitar a senha)
